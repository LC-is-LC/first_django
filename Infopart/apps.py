from django.apps import AppConfig

# #LC here is where we take config to add to the projrcfts settings.py file so it can know to check in the app for things
class InfopartConfig(AppConfig):
    name = 'Infopart'
